import { useParams } from "react-router-dom";
const SinglePage = () => {
    return (
        console.log(useParams());
        <div className="main_content">
            single page content
        </div>
    );
};